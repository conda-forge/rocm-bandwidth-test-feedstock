{% set name = "rocm-bandwidth-test" %}
{% set version = "2.10.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/RadeonOpenCompute/rocm_bandwidth_test/archive/roc-{{ version }}.tar.gz
  sha256: 1ae6e1258aa0b4510f3687edee2bdbb6eaa9d0dae18011678e8a2c50f412c464

build:
  number: 0
  skip: True  # [not linux]

requirements:
  build:
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
    - cmake
    - make
    - {{ cdt('numactl-devel') }}
    - {{ cdt('pciutils-devel') }}
  host:
    - rocr-runtime {{ version }}
    - roct-thunk-interface {{ version }}
  run:
    - rocr-runtime {{ version }}
    - roct-thunk-interface {{ version }}

test:
  commands:
    # Can't run without an AMD GPU
    - test -f $PREFIX/bin/rocm_bandwidth_test

about:
  home: https://github.com/RadeonOpenCompute/rocm_bandwidth_test
  license: NCSA
  license_family: MIT
  license_file: LICENSE.txt
  summary: 'Bandwidth test for ROCm'

extra:
  recipe-maintainers:
    - isuruf
